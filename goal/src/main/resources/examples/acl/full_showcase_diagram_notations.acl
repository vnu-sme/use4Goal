acl v2.0 Showcase {

  enum Status { A, B }

  abstract role Parent {
    attribute name : String;
  }

  role Manager extends Parent;
  role Employee;

  entity Task {
    attribute done : Boolean default false;
  }

  group Company {

    role Manager [1..1];
    role Employee [1..*];

    entity Task [0..*];

    subgroup Team [1..2] {
      role Employee [1..*];
    }

    compatibility Manager <-> Employee
      scope intra-group
      type compatible;

    relation Assign owns Manager -> Task
      scope intra-group;

    cardinality role Employee [2..10];
    cardinality entity Task [0..*];
    cardinality subgroup Team [1..2];
  }
}