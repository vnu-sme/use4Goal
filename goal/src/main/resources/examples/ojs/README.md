# OJS conformance example

`ojs_input.soil` is a valid pre-execution input fixture. Review recommendation and editor
decision start as `reject`; `ojs.bpmn2` produces their later accepted state, and `ojs.istar`
observes the root goal. The older `ojs_two_submissions.aol` remains a standalone snapshot
example and is not treated as a behavioral trace.
