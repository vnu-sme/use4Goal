model InvalidOclMissingProperty {
  pool OrderOffice "Order Office" {
    start order_received : message ocl {[
      self.nonExistingFlag = true
    ]}

    end order_closed : none ocl {[
      self.status = #approved
    ]}

    flow order_received -> order_closed
  }
}
